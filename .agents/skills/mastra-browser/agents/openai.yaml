interface:
  display_name: "Mastra Browser"
  short_description: "Repo-specific patterns and workflows for mastra-browser"
  default_prompt: "Use the mastra-browser repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true